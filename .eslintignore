node_modules
dist
release
.turbo

# Ankama's shell and the vendored keymaster library. Not our code, and it is
# shipped verbatim - reformatting it would make the regex patches harder to
# compare against what the game actually loads.
apps/*/game-base

# generated by typesafe-i18n
packages/i18n/formatters.ts

# build output of the apps
apps/*/dist
apps/*/.expo
